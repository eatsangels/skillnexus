name: Build/Publish Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  publish:
    runs-on: windows-latest

    steps:
      - name: Checkout git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install root dependencies
        run: npm install

      - name: Install frontend dependencies
        run: npm --prefix skill-dashboard/frontend install

      - name: Install backend dependencies
        run: npm --prefix skill-dashboard/backend install

      - name: Run backend tests (quality gate)
        run: npm --prefix skill-dashboard/backend test

      - name: Build frontend assets
        run: npm run build:frontend

      - name: Prune backend devDependencies (no empaquetar Vitest en el asar)
        run: npm --prefix skill-dashboard/backend prune --omit=dev

      - name: Build and Publish Electron App
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --win --publish always
